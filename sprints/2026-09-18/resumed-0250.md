# Resumed sprint, September 18 02:50

Fresh account resumed the same sprint. User decisions remain grant preservation under new rules and cloud/resource-host lifecycle for Wisper. A separate optional question asks whether management should be recoverable under a distinct owner policy or intentionally self-governing; no answer is recorded yet. Work independent of that choice continues.

Bread **ff11b7578** fixes the foreign-thread test harness to use pthreads under the Lean-selected C++ runtime. The actual hbox/native-Lean combined suite now **passes all 9 selected tests in 1.213s**: narrow real DelegAdmit, failure/unwind/mode checks and all 5 factory chronology tests. Factory creation/mutation/reopen/history/forks took 0.737s. The source snapshot is exact and includes preexisting formatting-only reversible.rs WIP; do not mislabel it as a pristine-HEAD run. Full/ST initialization, unchanged full-byte PQ, actual Hermes first-use profiling and affected history regressions remain in progress.

Mini **a02aa80** adds receiveLoaded: publish only against the exact image used for admission, one physical CAS, no rebase after contention, existing exact-intent readback on success/uncertainty. Its actual internal Lean+SQLite lifecycle probe passes 7 commits with crash/response-loss recovery, refusal, replay and a new clock race. The concurrent operation keeps all physical bytes/guards unchanged while advancing the journal; stale pinned admission refuses contention, explicit fresh admission succeeds once. The first new test fixture used an empty write set and correctly hit noCells; it was changed to a nonempty identity write without weakening any gate.

The coordinated policyRevision/generation and explicit-delegation migration is active in the live Mini tree. Foundation, common 17-field declaration, mixed-lineage checker and policy-admission components have passed narrow checks; the complete receiving journey and root/umbrella have not yet passed at this new schema. A discovered codec→backend→manifest import cycle is being repaired by moving the pure request projection into its existing candidate-independent source module. This is implementation convergence, not a completed compatibility migration.

Host work is now actual source, not only a proposal: concrete checked-native BabyBear/29 profile, strict calls/signing/result codecs, configurable source-owned genesis and compiled CLI/stdio surface. These new host files remain uncompiled at this checkpoint. Query/delegation endpoints are exposed only once real authorization consumers exist; no public deployment or succinct-STARK readiness claim follows.

[Exact captured evidence](resumed-0250/inventory.json) retains source identities, commands, results and the earlier fixture refusal. The preceding account-handoff remains historical and includes the unapplied drafts from which the new live work started.
