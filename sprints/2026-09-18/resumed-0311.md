# Resumed sprint, September 18 03:11

Ember selected deliberate self-governance of management: a resource may deny further policy changes, including its owner's. There is no implicit owner repair bypass. Existing grants survive source revisions but remain subject to the current rules. The installer keeps an explicit management-lockout case.

Mini **4095dc6**, **ed8d6ba** and **481def3** checkpoint the coordinated revision/generation and explicit-delegation foundations, canonical physical authority and signature/artifact consumers, and birth/native profile. Exact source manifests and scoped logs are [captured here](resumed-0311/inventory.json). Focused source checks include 44 foundation and 28 admission axiom pins, 34 birth declarations, and the actual BabyBear/29 range contract. Whole-root integration, generated Rust consumers and the joined receiving journey remain separate gates.

The actual native signature probe passes using real Ed25519 and reopened SQLite authority. It checks independent source revision, revision-only signature tampering, genuinely signed legacy frame/domain refusal, missing seventeenth field, and the standing key/revocation/replay cases. This is a receiving result, not only a source check. Current birth, invocation, policy installation and delegation modules subsequently reached their narrow source gates; the joined native create → two rule updates → retained owner grant → delegated recipient invocation is running next.

Independent host review found that unsigned preparation exposed a balance threshold oracle through affordability failures. Public unsigned preparation is closed while actual per-resource signed observation/preparation is implemented. Fresh mutations retain blind-write semantics and return uniform public refusal; private diagnostic detail requires read authority. Public challenge metadata includes configuration, commitments, heights, enrolled public key records, policy counters and address/allocation occupancy; balances and resource payloads are not made public. No timing noninterference claim is made.

The same review distinguishes physical journal replay from rechecking historical semantic authorization. Source work is now assigned to verify each original signed operation against its historical prefix and compare the exact source-derived intent. The Book's native law also needs a support invariant so unregistered hidden balances cannot enter a supposedly canonical image. These are active core repairs, not accepted release limitations. The host is not yet linked or a completed public interface.

Bread's original full-init lifecycle baseline passes all three fresh-process cases in **564.570s**, with individual startup around 187–189s. The exact game fixture separation then passes both existing fixture modules and the complete FFI dependency build: all moved definitions and proof checks are retained. Generated-C inspection confirms 42 NetworkJudge eager definitions leave the runtime module and six DeckDescent search definitions leave its runtime module. Executor-only native initialization subsequently passes its scoped check at approximately **41ms**; the full changed runtime/PQ/Hermes measurements remain in progress.

The existing direct/JSON corpus was found to exercise refusal paths because its fixed previous receipt does not match its hosts' heads. It remains a codec/refusal check; it is not credited with accepted-action fidelity. An independent source audit found full-width credential digests narrowed by the direct ABI. Actual accepted and high-bit cases plus full-width builders are now assigned. No corpus was removed or weakened.
