# Shared authority foundation migration

The live source is checked in seven owned Theory files, each frozen in the companion JSON. This is not native receiver or umbrella closure.

- Request/AuthState now carry policyRevision independently of exact policyEpoch generation. Authorized requires current revision in every mode; source lookup is revision-based. The executable declaration has 17 fields, schemaVersion4 and a common revision check. No revision defaults exist on wire or generic request constructors.
- Existing grants retain their exact immutable capability scopes, holder, time, generation and revocation constraints across a source-only revision. A general theorem makes those preserved projections explicit; new signatures, current-source membership and new predicate acceptance remain necessary. Concrete generation5/revision11 and stale-revision/rotation refusal witnesses are checked.
- Actual delegated lineage replaces bare parents with ParentLink + strict/delegated fullRequest origins. StrictAttenuates and its holder-subset theorem remain unchanged. General root-admissible theorem is strict-only; mixed lineage promises payload bounds, not grantor-holder authorization of recipient use.
- Effects uses mandatory actual exact-parent capability-mode authorization inside DelegationEvidence. DelegationContext.request codec effectDigest pre declaration derives root and current revision from the actual same pre. Global all-kind capability ID freshness covers issuance and descent; canonical parent/suffix anchoring is mandatory and retained at actual joint post.
- Policy-generation rotation now frames target revision and every versioned address in its mandatory actual-joint-post condition (EpochTarget.SourceFramed), with ordinary-axiom proofs. This does not add a receiver, a rotation verb or a management recovery bypass. An intentional source+generation batch must have its own ordered semantics.
- Forty-four live declaration axiom checks pass with only ordinary Lean axioms; permanent exact pins in Assurance/CredentialFoundationMigrationAudit.lean also emitted GREEN (exit0, empty log).

Owner follow-ups: authority handles all physical codecs/Entry/Domain projection; admission owns mixed checker and source gate; journey owns actual native delegation/use; installer preserves generation in actual final policy update; root owns umbrella import and full native receiving closure. No Git operations performed here.

Canonical source projection addition: resume_signatures moved pure requestCodecVersion2/requestWords/requestWireOfWords and injectivity into AuthorizationDeclaration, breaking the lowcodec→backend→artifact/manifest cycle. Its final exact GREEN check supersedes my earlier declaration measurement; both hashes/checks remain in the JSON. No semantic gate was weakened.

No active compiler; seat1 released to resume_authority after final permanent audit. Root owns commit/push and umbrella import of the new assurance module.
