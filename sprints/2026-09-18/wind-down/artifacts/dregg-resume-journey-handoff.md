# Actual resource delegation journey — resume checkpoint

The actual native joined witness passed with process exit 0. It is a ZMod65537/scalar15 fixture with explicitly public test keys and the real native Ed25519 verifier and SQLite store, not a deployed production parameter claim.

Source `scripts/probe-born-resource-invocation.lean` now executes paid source-derived resource birth, the actually issued owner grant, two installed rule revisions using the original control grant, retained owner/control bytes and unchanged grant generation, a new-rule refusal of the owner operation, actual parent-authorized Alice-to-Bob narrower authority production, Bob's fresh signature and current revision invocation, and persisted reopen. Exact original birth/install/delegation/invocation receipts replay after expiry without changing the final storage image. Noncanonical input, stolen owner grant, changed recipient under the original signature, amplified child, cross-kind duplicate child identity, wrong recipient signature and child redelegation without the delegate verb refuse.

`Kernel/CapabilityDelegationController.lean` has one existing PreparedTuple authority incidence; current policy observes its exact complete old/post authority state and unchanged target. Its accepted mode requires actual capability-only authorization naming the canonical parent. It produces the explicit delegated historical request/lineage, with global capability-ID freshness and shared lineage checking. Seven named controller laws passed the ordinary-three-axiom audit. `Kernel/CapabilityDelegationReceiver.lean` was completed and audited by resume_birth_review, including installed complete authority pages/catalogue and unchanged target/source laws.

The invocation controller now selects policy revision separately from grant generation, retains exact equality checks, and publishes from the same loaded image used for admission. It also distinguishes actual compiler input-range/cast-alias refusals. Source compiled green. During the native test, resume_birth_review factored its signed-ingress codec to add canonical decoding for native history verification. The native run consumed the earlier emitted controller, whose exact ca22fd700... source is preserved at `/tmp/dregg-declared-before-history-codec.lean`. The concurrent factor changed no admission or receive logic; its encoding equality proof/check is owned by that agent. Do not call the native result an exact hash match to the post-factor file.

Evidence: `/tmp/dregg-resume-journey-checks.json`, `/tmp/dregg-resume-born-joined-source.json`, `/tmp/dregg-resume-born-joined-native.log`, `/tmp/dregg-delegation-controller-audit.log`. The native run took about535 seconds under Lean's IR interpreter; that is not compiled host latency. The new compiled host/CLI/authenticated-query/history path belongs to other agents and is not certified by this result.

Wind-down: all sources owned by this lane are frozen; no process remains. The one-line Entry.policy arity migration in `scripts/probe-declared-resource.lean` remains source-only, uncompiled/unrun this wave. Root owns commits. No Git, branch, worktree, stash or rollback was run by this agent.
