# Full-request revision signing and artifact lane

The source request is seventeen fields, with policyRevision immediately after policyEpoch and before cost. Generation remains exact and source revision is independently signed. Theory.AuthorizationDeclaration now owns the pure request word, inverse, length, injectivity and requestCodecVersion = 2. Compiler.TypedAuthorizationRequestCodec supplies the one canonical byte codec and forwarding aliases. This placement avoids the actual dependency cycle through Tower256ConcreteBackend → BinaryTower256Profile → MinidreggV1Artifact → SemanticArtifactBundle/SemanticManifest. The scoped local import scan found zero cycles among 554 modules after the change.

CredentialSignatureAdmission.requestBytes forwards the one low codec; requestFrame is version 2 and signatureDomain is version 2. The generic signed envelope shape/version is unchanged. Universal native request-byte equality and revision separation pass ordinary-axiom guards. The joint artifact is schema 2 and owns a universal equality to the same signed request projection. Signature admission no longer imports the entire declared-hyperedge kernel merely to obtain this equality. Shared operation nullifier and birth identity algorithms were not changed by this lane.

Source duplication was removed from SemanticManifest.RequestEncoding (now the source word list with a refusing inverse) and Assurance.CredentialTokenLocalEndpoint (now the low wire codec, endpoint version 2). Hyperdocument RequestEnvelope, declared-action RequestContext, and declared-hyperedge Seed now require explicit revision and forward it. The declared-action AIR's public policy bytes include the revision. Semantic authorization artifact schema 4 has seventeen field tags and a mandatory source revision check in every mode; a named theorem verifies this. Concrete artifact manifest pins derive declaration version 4 and request codec version 2 from those source definitions.

Actual runtime: /tmp/dregg-resume-native-signature.log has PASS and exit 0. The real Ed25519 verifier and public test signer operate against a full authority snapshot loaded after native SQLite persistence/reopen. The probe covers revision-only request mutation, changed signed frame with the old signature, actually signed old request framing and old signature domain, omitted revision (old sixteen-field shape), wrong key/epoch/domain, canonical framing, revoked/expired/not-yet-active keys, authority substitution and shared nullifier consumption. It does not claim a full policy, delegation or resource journey. All recorded source and binary hashes still matched immediately after this run; inputs are /tmp/dregg-resume-native-signature-source.json.

The authoritative narrow check manifest is /tmp/dregg-resume-signatures-checkpoint.json: thirteen emitted modules are green, with exact source/log hashes, and the native probe is recorded separately. This includes consumer-owned Kernel/DeclaredHyperedgeWitness.lean (compiled here, not edited here) and authority-owned low codec (only mechanical forwarding-alias proof repairs made here with authorization). Final Declaration source hash was sent to foundations and its superseding check is in their manifest. The source-owned JSON emitter ran and produced prover/testdata/semantic_artifact_v1.json; inspection confirmed schema 4, field tags 1..17, revision check tag 16 in all three modes, and codec pins 4/2.

Remaining checks, awaiting the coordinated compiler seat: Compiler/DeclaredActionBytes → Compiler/DeclaredActionAir; Assurance/CredentialTokenLocalEndpoint after the current Assurance/DeployedCredentialLifecycle dependency; Compiler/NativeGlueGen → Compiler/MinidreggV1NativeGlue; Compiler/ArithmeticNativeDeployment; Compiler/EvmStage0NativeDeployment. The latter three real Lean emitters own prover/generated/semantic_artifact_v1.rs, prover/src/semantic_artifact_arithmetic.rs and prover/generated/evm_stage0_add_aux.rs. Do not hand-edit these outputs. Their real Rust consumers still need an appropriate focused native-dispatch check after regeneration. Do not include foreign prover/lib.rs, Cargo, Uwueave, Compiler.lean or licensing work. Root owns Git; no branches, worktrees, stash or Git operations performed by this lane.

Compiler seat 2 was explicitly released to resume_authority after the native probe. No process from this lane remains active. Native executables: /tmp/minidregg-credential-signature-verifier-target/release/minidregg-credential-signature-verifier; sibling examples/sign-probe; /Users/ember/dev/minidregg/native/hyperdocument-link-sqlite-store/target/release/minidregg-link-sqlite-store. Use at most two narrow Lean processes globally for Mini, each LEAN_NUM_THREADS=2. Do not run an umbrella build mid-layout migration.
