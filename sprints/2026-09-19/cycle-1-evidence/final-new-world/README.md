# Original native New World cycle: completed public run

The immutable original cycle used source commit `12e66083e2d028133f236d62c24bfe02af3b861e`, native host SHA256 `0da9f1392fad3913356e3a495f0d3ab4f1a247aec8988a9f90312666e9ccadc1`, and compiled acceptance runner SHA256 `dbd2539ac5a91eb4dec6f381f41fd2cba276214cc8ed637c1221fba7537a994e`. `manifest.txt` is copied byte-for-byte, preserving the original command, artifact hashes, whitespace, UTC start/end, exit code, and elapsed time. All six named runtime artifacts (runner, host, credential verifier, SQLite store helper, OpenSSL, and probe source) were independently rehashed after the run and matched the original manifest (`runtime-artifact-rehash.tsv`).

The run began at `2026-09-19T21:03:47Z` and ended at `2026-09-19T23:56:48Z`; its wrapper measured `10,380` seconds elapsed and exit code `0`. `summary.json` reports `PASS` and eleven accepted events. The exact final `PASS NEW WORLD CYCLE` marker, historical retry outcomes, current-law read refusals with equal public refusal bytes, deliberate management lockout, denied owner repair, and unchanged durable image assertion appear in the full public `run.log`. `completion-checks.txt` records the gate checks.

The final stored image was 115,541 bytes, SHA256 `6b4173721a134c5658c6f7e7843cd409b2fd34e563b6328399ddaeafb9cd20f3`. Its bytes are **not** archived. The image equality asserted by the run compares the full logical stored image returned by the SQLite helper, not arbitrary raw SQLite database file bytes.

This is actual native execution with fresh fixture keys, a synthetic local genesis, detached Ed25519 custody, the compiled public host, and SQLite. It does not establish a hosted service, provider dispatch, physical interruption recovery, STARK/succinct proof, or real-asset settlement. No private keys, config files, signed calls, store database, or image bytes are in this archive. Assortia INDEX, SHA256SUMS, and graph are maintained separately by the root lane.
